# Open WebUI 技术文档

## 项目概述

**Open WebUI** 是一个可扩展、功能丰富且用户友好的自托管 AI 平台，设计为完全离线运行。它支持多种 LLM 运行器（如 Ollama 和 OpenAI 兼容的 API），并内置 RAG（检索增强生成）推理引擎，是一个强大的 AI 部署解决方案。

### 核心特性

- 🚀 **易于部署**：支持 Docker 和 Kubernetes 一键安装
- 🤝 **多 API 集成**：支持 Ollama、OpenAI 兼容 API（LMStudio、GroqCloud、Mistral、OpenRouter 等）
- 🛡️ **细粒度权限控制**：支持用户组和角色管理
- 📱 **响应式设计**：支持桌面、笔记本和移动设备
- 📱 **PWA 支持**：移动端原生应用体验
- ✒️🔢 **Markdown 和 LaTeX 支持**：完整的格式化能力
- 🎤📹 **语音/视频通话**：支持多种语音识别和合成引擎
- 🛠️ **模型构建器**：通过 Web UI 轻松创建 Ollama 模型
- 🐍 **Python 函数调用工具**：内置代码编辑器，支持自定义 Python 函数
- 💾 **持久化存储**：内置键值存储 API，支持会话数据持久化
- 📚 **本地 RAG 集成**：支持 9 种向量数据库和多种内容提取引擎
- 🔍 **Web 搜索**：支持 15+ 搜索提供商
- 🌐 **网页浏览**：支持将网页内容直接集成到聊天中
- 🎨 **图像生成与编辑**：支持 DALL-E、Gemini、ComfyUI、AUTOMATIC1111
- ⚙️ **多模型对话**：同时与多个模型交互
- 🔐 **RBAC**：基于角色的访问控制
- 🗄️ **灵活的数据库选项**：SQLite、PostgreSQL、云存储后端（S3、GCS、Azure Blob）
- 🔍 **高级向量数据库支持**：ChromaDB、PGVector、Qdrant、Milvus、Elasticsearch、OpenSearch、Pinecone、S3Vector、Oracle 23ai
- 🔐 **企业级认证**：LDAP/Active Directory、SCIM 2.0、SSO、OAuth
- ☁️ **云原生集成**：Google Drive、OneDrive/SharePoint 文件选择
- 📊 **生产可观测性**：内置 OpenTelemetry 支持
- ⚖️ **水平扩展**：Redis 支持的会话管理和 WebSocket 多节点部署
- 🌐🌍 **多语言支持**：国际化（i18n）支持
- 🧩 **插件系统**：Pipelines 插件框架支持

## 技术架构

### 前端技术栈

- **框架**：SvelteKit 5.0+
- **语言**：TypeScript 5.5+
- **构建工具**：Vite 5.4+
- **样式**：Tailwind CSS 4.0+
- **UI 组件库**：
  - TipTap（富文本编辑器）
  - CodeMirror（代码编辑器）
  - Chart.js（图表）
  - Mermaid（流程图）
  - Leaflet（地图）
- **状态管理**：Svelte Stores
- **国际化**：i18next
- **实时通信**：Socket.IO Client
- **其他关键库**：
  - Pyodide（Python 运行时）
  - KaTeX（数学公式渲染）
  - Highlight.js（代码高亮）
  - PDF.js（PDF 查看）

### 后端技术栈

- **框架**：FastAPI 0.123.0
- **语言**：Python 3.11+（支持到 3.12）
- **ASGI 服务器**：Uvicorn 0.37.0
- **数据库 ORM**：
  - SQLAlchemy 2.0.38（主 ORM）
  - Peewee 3.18.3（备用 ORM）
- **数据库迁移**：Alembic 1.17.2
- **认证与安全**：
  - python-jose 3.5.0（JWT）
  - bcrypt 5.0.0（密码哈希）
  - argon2-cffi 25.1.0（密码哈希）
  - cryptography（加密）
  - authlib 1.6.5（OAuth）
- **会话管理**：
  - starsessions 2.2.1（Redis 支持）
  - python-socketio 5.15.0（WebSocket）
- **RAG 与向量数据库**：
  - langchain 0.3.27
  - langchain-community 0.3.29
  - chromadb 1.0.20
  - sentence-transformers 5.1.2
  - transformers 4.57.3
- **文档处理**：
  - unstructured 0.18.21
  - pypdf 6.4.0
  - python-pptx 1.0.2
  - docx2txt 0.8
  - openpyxl 3.1.5
- **语音处理**：
  - faster-whisper 1.1.1（语音识别）
  - soundfile 0.13.1
- **图像处理**：
  - pillow 11.3.0
  - opencv-python-headless 4.11.0.86
  - rapidocr-onnxruntime 1.4.4
- **云存储**：
  - boto3 1.41.5（AWS S3）
  - google-cloud-storage 2.19.0（GCS）
  - azure-storage-blob 12.24.1（Azure）
- **企业集成**：
  - ldap3 2.9.1（LDAP/AD）
- **任务调度**：APScheduler 3.10.4
- **缓存**：aiocache、Redis
- **HTTP 客户端**：
  - httpx 0.28.1（异步 HTTP）
  - requests 2.32.5（同步 HTTP）
  - aiohttp 3.12.15（异步 HTTP）

### 项目结构

```
open-webui/
├── backend/                    # 后端代码
│   ├── open_webui/            # 主应用包
│   │   ├── __init__.py        # 应用入口
│   │   ├── main.py            # FastAPI 应用主文件
│   │   ├── config.py          # 配置管理
│   │   ├── models/            # 数据模型
│   │   ├── routers/           # API 路由
│   │   ├── socket/            # WebSocket 处理
│   │   ├── retrieval/         # RAG 检索逻辑
│   │   ├── storage/           # 存储抽象层
│   │   ├── utils/             # 工具函数
│   │   ├── internal/          # 内部 API
│   │   ├── migrations/        # 数据库迁移
│   │   └── test/              # 测试代码
│   ├── data/                  # 数据目录
│   └── requirements.txt       # Python 依赖
├── src/                       # 前端代码
│   ├── lib/                   # 库文件
│   │   ├── components/        # Svelte 组件
│   │   ├── stores/            # 状态管理
│   │   ├── apis/              # API 客户端
│   │   ├── i18n/              # 国际化
│   │   └── utils/             # 工具函数
│   ├── routes/                # 路由页面
│   ├── app.css                # 全局样式
│   └── tailwind.css           # Tailwind 样式
├── static/                    # 静态资源
├── docs/                      # 文档
├── kubernetes/                # Kubernetes 部署配置
├── scripts/                   # 脚本文件
├── test/                      # 测试
├── cypress/                   # E2E 测试
├── package.json               # 前端依赖配置
├── pyproject.toml             # 后端依赖配置
├── Dockerfile                 # Docker 镜像构建
├── docker-compose.yaml        # Docker Compose 配置
├── Makefile                   # Make 命令
└── README.md                  # 项目说明
```

## 核心功能模块

### 1. 认证与授权

- **用户认证**：支持用户名/密码、OAuth、LDAP/AD、SSO
- **权限管理**：基于角色的访问控制（RBAC）
- **会话管理**：Redis 支持的分布式会话
- **安全特性**：
  - JWT Token 认证
  - 密码加密（bcrypt/argon2）
  - API 密钥管理
  - 审计日志

### 2. 模型管理

- **Ollama 集成**：本地模型管理
- **OpenAI 兼容 API**：支持多种 API 提供商
- **模型发现**：自动发现可用模型
- **模型配置**：支持多模型配置和负载均衡
- **模型构建器**：通过 Web UI 创建自定义模型

### 3. 对话系统

- **多模型对话**：同时与多个模型交互
- **流式响应**：实时流式输出
- **上下文管理**：会话历史管理
- **Markdown 渲染**：完整的 Markdown 和 LaTeX 支持
- **代码高亮**：语法高亮显示
- **文件上传**：支持多种文件格式

### 4. RAG（检索增强生成）

- **向量数据库支持**：
  - ChromaDB
  - PGVector（PostgreSQL）
  - Qdrant
  - Milvus
  - Elasticsearch
  - OpenSearch
  - Pinecone
  - S3Vector
  - Oracle 23ai
- **文档处理**：
  - 支持 PDF、Word、Excel、PowerPoint、Markdown 等
  - 多种内容提取引擎（Tika、Docling、Document Intelligence、Mistral OCR）
  - 外部加载器支持
- **检索策略**：
  - 语义搜索
  - 混合搜索（语义 + BM25）
  - 重排序
- **文档库管理**：文档上传、索引、搜索

### 5. Web 搜索与浏览

- **搜索提供商**：SearXNG、Google PSE、Brave Search、Kagi、Mojeek、Tavily、Perplexity、serpstack、serper、Serply、DuckDuckGo、SearchApi、SerpApi、Bing、Jina、Exa、Sougou、Azure AI Search、Ollama Cloud
- **网页浏览**：使用 `#` 命令集成网页内容

### 6. 语音与视频

- **语音识别（STT）**：
  - Local Whisper
  - OpenAI Whisper
  - Deepgram
  - Azure Speech
- **语音合成（TTS）**：
  - Azure
  - ElevenLabs
  - OpenAI
  - Transformers
  - WebAPI
- **视频通话**：集成视频通话功能

### 7. 图像生成与编辑

- **生成引擎**：
  - OpenAI DALL-E
  - Google Gemini
  - ComfyUI（本地）
  - AUTOMATIC1111（本地）
- **图像编辑**：基于提示的图像编辑

### 8. 函数调用与工具

- **Python 函数调用**：内置 Python 运行时（Pyodide）
- **代码编辑器**：支持 Python 代码编辑和执行
- **工具服务器**：支持外部工具服务器集成
- **MCP 协议**：Model Context Protocol 支持

### 9. 插件系统

- **Pipelines 框架**：插件开发框架
- **功能扩展**：
  - 函数调用
  - 速率限制
  - 使用监控
  - 实时翻译
  - 内容过滤
- **自定义逻辑**：支持 Python 库集成

### 10. 存储与持久化

- **数据库选项**：
  - SQLite（默认，支持加密）
  - PostgreSQL
- **云存储**：
  - AWS S3
  - Google Cloud Storage
  - Azure Blob Storage
- **会话存储**：Redis 支持的会话管理
- **文件存储**：本地或云存储后端

### 11. 可观测性

- **OpenTelemetry**：分布式追踪、指标、日志
- **审计日志**：操作审计记录
- **监控集成**：与现有监控栈集成

### 12. 企业功能

- **LDAP/AD 集成**：企业目录服务
- **SCIM 2.0**：自动化用户和组配置
- **SSO**：单点登录支持
- **OAuth**：OAuth 2.0 提供商集成
- **云存储集成**：Google Drive、OneDrive/SharePoint

## 部署方式

### 1. Docker 部署

#### 基础部署（Ollama 在本地）

```bash
docker run -d -p 3000:8080 \
  --add-host=host.docker.internal:host-gateway \
  -v open-webui:/app/backend/data \
  --name open-webui \
  --restart always \
  ghcr.io/open-webui/open-webui:main
```

#### Ollama 在远程服务器

```bash
docker run -d -p 3000:8080 \
  -e OLLAMA_BASE_URL=https://example.com \
  -v open-webui:/app/backend/data \
  --name open-webui \
  --restart always \
  ghcr.io/open-webui/open-webui:main
```

#### GPU 支持

```bash
docker run -d -p 3000:8080 \
  --gpus all \
  --add-host=host.docker.internal:host-gateway \
  -v open-webui:/app/backend/data \
  --name open-webui \
  --restart always \
  ghcr.io/open-webui/open-webui:cuda
```

#### 仅使用 OpenAI API

```bash
docker run -d -p 3000:8080 \
  -e OPENAI_API_KEY=your_secret_key \
  -v open-webui:/app/backend/data \
  --name open-webui \
  --restart always \
  ghcr.io/open-webui/open-webui:main
```

#### 包含 Ollama 的完整部署（GPU）

```bash
docker run -d -p 3000:8080 \
  --gpus=all \
  -v ollama:/root/.ollama \
  -v open-webui:/app/backend/data \
  --name open-webui \
  --restart always \
  ghcr.io/open-webui/open-webui:ollama
```

### 2. Docker Compose 部署

使用项目提供的 `docker-compose.yaml`：

```bash
docker-compose up -d
```

### 3. Python pip 安装

**要求**：Python 3.11+

```bash
pip install open-webui
open-webui serve
```

访问：http://localhost:8080

### 4. Kubernetes 部署

#### 使用 Kustomize

```bash
# CPU 版本
kubectl apply -f ./kubernetes/manifest/base

# GPU 版本
kubectl apply -k ./kubernetes/manifest
```

#### 使用 Helm

```bash
# 打包
helm package ./kubernetes/helm/

# CPU 版本
helm install ollama-webui ./ollama-webui-*.tgz

# GPU 版本
helm install ollama-webui ./ollama-webui-*.tgz \
  --set ollama.resources.limits.nvidia.com/gpu="1"
```

### 5. Makefile 命令

```bash
make install      # 安装并启动
make start        # 启动服务
make stop         # 停止服务
make update       # 更新服务
make remove       # 移除服务
```

## 配置说明

### 环境变量

主要环境变量配置：

- `OLLAMA_BASE_URL`：Ollama 服务地址
- `OPENAI_API_KEY`：OpenAI API 密钥
- `OPENAI_API_BASE_URLS`：OpenAI 兼容 API 地址
- `WEBUI_SECRET_KEY`：WebUI 密钥
- `WEBUI_NAME`：WebUI 名称
- `REDIS_URL`：Redis 连接 URL
- `DATABASE_URL`：数据库连接 URL
- `ENABLE_OLLAMA_API`：启用 Ollama API
- `ENABLE_OPENAI_API`：启用 OpenAI API
- `ENABLE_BASE_MODELS_CACHE`：启用模型缓存
- `ENABLE_OTEL`：启用 OpenTelemetry
- `HF_HUB_OFFLINE`：离线模式（设置为 1）

### 数据库配置

#### SQLite（默认）

无需额外配置，数据存储在 `/app/backend/data` 目录。

#### PostgreSQL

```bash
DATABASE_URL=postgresql://user:password@host:5432/dbname
```

#### 向量数据库配置

根据选择的向量数据库，配置相应的连接参数。

## 开发指南

### 本地开发环境设置

#### 前端开发

```bash
npm install
npm run dev
```

前端运行在：http://localhost:5173

#### 后端开发

```bash
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -e .
open-webui dev
```

后端运行在：http://localhost:8080

### 代码规范

- **前端**：使用 ESLint 和 Prettier
- **后端**：使用 Black 和 Pylint
- **TypeScript**：使用 svelte-check 进行类型检查

### 测试

```bash
# 前端测试
npm run test:frontend

# E2E 测试
npm run cy:open

# 代码检查
npm run lint
npm run check
```

## API 文档

### 主要 API 端点

- `/api/v1/`：主要 API 端点
- `/api/v1/chat`：聊天 API
- `/api/v1/models`：模型管理 API
- `/api/v1/users`：用户管理 API
- `/api/v1/documents`：文档管理 API
- `/api/v1/retrieval`：RAG 检索 API
- `/internal/`：内部 API

### WebSocket

- Socket.IO 连接：实时通信
- 事件：模型更新、消息流、状态更新

## 安全考虑

1. **认证**：使用 JWT Token 和安全的密码哈希
2. **授权**：基于角色的访问控制
3. **数据加密**：支持数据库加密
4. **API 安全**：API 密钥管理和速率限制
5. **审计日志**：操作审计记录
6. **CORS**：可配置的跨域策略

## 性能优化

1. **缓存**：模型列表缓存、Redis 缓存
2. **异步处理**：使用异步 I/O 提高性能
3. **水平扩展**：支持多节点部署
4. **数据库优化**：连接池、索引优化
5. **静态资源**：CDN 支持

## 故障排除

### 常见问题

1. **连接问题**：检查 Ollama 服务地址和网络配置
2. **数据库问题**：检查数据库连接和权限
3. **GPU 问题**：检查 CUDA 和 Docker GPU 支持
4. **权限问题**：检查文件权限和用户权限

### 日志

- 应用日志：使用 Loguru 记录
- 访问日志：HTTP 访问日志
- 错误日志：错误追踪和调试信息

## 更新与维护

### 更新 Docker 镜像

```bash
docker pull ghcr.io/open-webui/open-webui:main
docker-compose down
docker-compose up -d
```

### 使用 Watchtower 自动更新

```bash
docker run --rm \
  --volume /var/run/docker.sock:/var/run/docker.sock \
  containrrr/watchtower \
  --run-once open-webui
```

## 社区与支持

- **GitHub**：https://github.com/open-webui/open-webui
- **Discord**：https://discord.gg/5rJgQTnV4s
- **文档**：https://docs.openwebui.com/
- **社区**：https://openwebui.com/

## 许可证

项目包含多个许可证。详细信息请参考：
- `LICENSE`：当前许可证
- `LICENSE_HISTORY`：许可证历史记录

## 贡献指南

欢迎贡献代码！请参考：
- 代码规范
- 提交指南
- 行为准则（CODE_OF_CONDUCT.md）

## 版本信息

- **当前版本**：0.6.41
- **Python 要求**：3.11 - 3.12
- **Node.js 要求**：18.13.0 - 22.x.x

---

**创建者**：Timothy Jaeryang Baek

**最后更新**：2024年

